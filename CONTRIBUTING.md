# Contributing

## Setup
```bash
make install
make test
```

## Guidelines
- Cover new logic with a test in `tests/`.
- Keep cost assumptions in `src/config.py`.
- Run `make lint` and `make test` before opening a PR.
